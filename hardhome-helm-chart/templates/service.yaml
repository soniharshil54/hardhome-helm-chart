apiVersion: v1
kind: Service
metadata:
  name: {{ include "hardhome-helm-chart.fullname" . }}
  labels:
    {{- include "hardhome-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hardhome-helm-chart.selectorLabels" . | nindent 4 }}
